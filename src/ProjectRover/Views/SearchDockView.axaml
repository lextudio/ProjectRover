<UserControl x:Class="ProjectRover.Views.SearchDockView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ProjectRover.ViewModels"
             xmlns:searchResults="clr-namespace:ProjectRover.SearchResults"
             xmlns:converters="clr-namespace:ProjectRover.Converters"
             x:DataType="vm:MainWindowViewModel">
  <DockPanel>
    <Grid DockPanel.Dock="Top"
          Height="2">
      <ProgressBar IsVisible="{Binding IsSearching}"
                   IsIndeterminate="True"
                   Height="2"
                   MinHeight="2"
                   Background="Transparent"
                   CornerRadius="0" />
    </Grid>
    <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto" Margin="5">
      <TextBox x:Name="SearchTextBox"
               Watermark="Search..."
               Text="{Binding SearchText}"
               Margin="0,0,8,0">
        <TextBox.InnerRightContent>
          <Panel Margin="0,0,5,0">
            <Button Background="Transparent"
                    BorderBrush="{x:Null}"
                    Padding="2"
                    Click="OnClearSearchClick"
                    IsVisible="{Binding SearchText, Converter={x:Static converters:StringConverters.IsNotNullOrEmpty}}"
                    Cursor="Hand">
              <Svg Path="{DynamicResource CloseIcon}" Width="14" Height="14" />
            </Button>
            <Svg Path="{DynamicResource SearchIcon}"
                 Width="14"
                 Height="14"
                 IsVisible="{Binding SearchText, Converter={x:Static converters:StringConverters.IsNullOrEmpty}}" />
          </Panel>
        </TextBox.InnerRightContent>
      </TextBox>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="4">
        <TextBlock Text="Search for:" VerticalAlignment="Center" />
        <ComboBox Width="140"
                  ItemsSource="{Binding SearchModes}"
                  SelectedItem="{Binding SelectedSearchMode}"
                  VerticalAlignment="Center">
          <ComboBox.ItemTemplate>
            <DataTemplate DataType="vm:SearchMode">
              <StackPanel Orientation="Horizontal" Spacing="6">
                <Svg Width="16" Height="16" Path="{Binding IconKey, Converter={StaticResource IconKeyToPathConverter}}" />
                <TextBlock Text="{Binding Name}" />
              </StackPanel>
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>
      </StackPanel>
    </Grid>
    <TextBlock DockPanel.Dock="Top"
               Text="{Binding NumberOfResultsText}"
               IsVisible="{Binding NumberOfResultsText, Converter={x:Static converters:StringConverters.IsNotNullOrEmpty}}"
               Margin="10 5 0 5"
               Foreground="#919191"
               FontSize="12"/>
    <Grid x:Name="HeaderGrid" DockPanel.Dock="Top" Margin="5,0,5,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="220" />
        <ColumnDefinition Width="4" />
        <ColumnDefinition Width="220" />
        <ColumnDefinition Width="4" />
        <ColumnDefinition Width="150" />
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="Name" FontWeight="Bold" HorizontalAlignment="Center" />
      <Rectangle Grid.Column="1" Fill="{DynamicResource ThemeForegroundLowBrush}" Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" />
      <GridSplitter Grid.Column="1" Background="{DynamicResource ThemeForegroundLowBrush}" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" />
      <TextBlock Grid.Column="2" Text="Location" FontWeight="Bold" HorizontalAlignment="Center" />
      <Rectangle Grid.Column="3" Fill="{DynamicResource ThemeForegroundLowBrush}" Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" />
      <GridSplitter Grid.Column="3" Background="{DynamicResource ThemeForegroundLowBrush}" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext" />
      <TextBlock Grid.Column="4" Text="Assembly" FontWeight="Bold" HorizontalAlignment="Center" />
    </Grid>
    <ListBox ItemsSource="{Binding SearchResults}"
             SelectedItem="{Binding SelectedSearchResult}"
             Background="Transparent"
             BorderThickness="0"
             DoubleTapped="OnSearchResultDoubleTapped">
      <ListBox.ItemTemplate>
        <DataTemplate DataType="searchResults:BasicSearchResult">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="{Binding #HeaderGrid.ColumnDefinitions[0].Width}" />
              <ColumnDefinition Width="4" />
              <ColumnDefinition Width="{Binding #HeaderGrid.ColumnDefinitions[2].Width}" />
              <ColumnDefinition Width="4" />
              <ColumnDefinition Width="{Binding #HeaderGrid.ColumnDefinitions[4].Width}" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" ColumnDefinitions="Auto,*">
               <Svg Width="16" Height="16" Path="{Binding IconPath}" Margin="0,0,6,0" />
               <TextBlock Grid.Column="1" Text="{Binding Name}" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Name}" VerticalAlignment="Center" />
            </Grid>
            <Rectangle Grid.Column="1" Fill="{DynamicResource ThemeForegroundLowBrush}" Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" />
            <Grid Grid.Column="2" ColumnDefinitions="Auto,*">
               <Svg Width="16" Height="16" Path="{Binding LocationIconPath}" Margin="0,0,6,0" />
               <TextBlock Grid.Column="1" Text="{Binding Location}" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Location}" VerticalAlignment="Center" />
            </Grid>
            <Rectangle Grid.Column="3" Fill="{DynamicResource ThemeForegroundLowBrush}" Width="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" />
            <Grid Grid.Column="4" ColumnDefinitions="Auto,*">
               <Svg Width="16" Height="16" Path="{Binding AssemblyIconPath}" Margin="0,0,6,0" />
               <TextBlock Grid.Column="1" Text="{Binding Assembly}" TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding Assembly}" VerticalAlignment="Center" />
            </Grid>
          </Grid>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </DockPanel>
</UserControl>
