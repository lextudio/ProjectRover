mode: ContinuousDelivery
tag-prefix: 'v'

branches:
  master:
    regex: master
    increment: Patch
  main:
    regex: main
    increment: Patch
  local-test:
    regex: local-test
    increment: Patch

ignore:
  sha: []
